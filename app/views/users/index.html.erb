<div class="container-fluid">
  <p id="notice"><%= notice %></p>

  <h1><%= t('.players') %></h1>

  <table class="table">
    <thead>
      <tr>
        <th><%= t('.username') %></th>
        <th><%= t('.created_matches') %></th>
        <th><%= t('.played_matches') %></th>
        <th><%= t('.added_games') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to(user.username, user) %></td>
          <td><%= user.matches.size %></td>
          <td><%= user.played_matches.size %></td>
          <td><%= user.games.size %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= paginate @users %>
</div>